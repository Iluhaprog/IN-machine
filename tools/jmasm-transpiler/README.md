Данный инструмент позволяет транспилировать asm код в код исполняемый моделью
в логисиме или для запуска непосредственно на физическом устройстве.

Установка
```
npm install -g .
```

Примеры можно посмотреть в директории **examples/**